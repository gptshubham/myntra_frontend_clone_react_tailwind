import React from 'react';
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.querySelector('#root'));

const check = (
  <h1 class="text-3xl font-bold text-[#f1f2f3] bg-[#333] p-[10px]">Myntra</h1>
);

root.render(check);
